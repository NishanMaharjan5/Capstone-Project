// Trip's own category set — kept separate from CATEGORIES (the main app's list)
// so trips only ever show the categories relevant to a trip. Matches backend
// TRIP_CATEGORIES exactly.
export const TRIP_CATEGORIES = ['Eating Out', 'Transport & Vehicle', 'Accommodation', 'Entertainment', 'Shopping', 'Other']
